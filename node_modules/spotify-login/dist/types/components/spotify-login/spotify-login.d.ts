import { EventEmitter } from "../../stencil-public-runtime";
export declare class SpotifyLogin {
    /**
     * Client ID for Spotify OAuth application
     */
    clientId: string;
    /**
     * 	The URI to redirect to after the user grants or denies permission.
     */
    redirectUri: string;
    /**
     * Scope for Spotify OAuth application
     */
    responseType: string;
    /**
     * Scope for Spotify OAuth application
     */
    scope: string;
    /**
     * The state can be useful for correlating requests and responses
     */
    state: string;
    /**
     * Whether or not to force the user to approve the app again if they’ve already done so.
     */
    showDialog: boolean;
    /**
     * Call with request
     */
    request: EventEmitter;
    /**
     * Call with success
     */
    completed: EventEmitter;
    /**
     * Call with error
     */
    fail: EventEmitter;
    validateClientId(newValue: string): void;
    validateRedirectUri(newValue: string): void;
    validateResponseType(newValue: string): void;
    urlSpotify: string;
    popup: any;
    interval: number;
    private convertQueryParams;
    private close;
    private poll;
    private onBtnClick;
    render(): any;
}
